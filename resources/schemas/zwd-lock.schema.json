{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://zwd.local/schemas/zwd-lock.schema.json",
  "title": "Zed Workspace Dock Lock",
  "description": "Lock file stored as .zwd-lock.json inside a managed symlink dock.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "workspace_path", "links"],
  "properties": {
    "version": {
      "description": "Lock format version supported by this binary.",
      "type": "integer",
      "const": 1
    },
    "workspace_path": {
      "description": "Absolute path of the workspace file that owns this dock.",
      "type": "string",
      "minLength": 1
    },
    "links": {
      "description": "Symlink entries managed by this dock.",
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "target"],
        "properties": {
          "name": {
            "description": "Single filesystem entry name inside the dock.",
            "type": "string",
            "minLength": 1
          },
          "target": {
            "description": "Absolute path targeted by the dock symlink.",
            "type": "string",
            "minLength": 1
          }
        }
      }
    }
  }
}
